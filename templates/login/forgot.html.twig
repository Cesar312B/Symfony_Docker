{% extends 'login_base.html.twig' %}

{% block title %}Hello ForgotController!{% endblock %}


    {% block body %}
<div class="card-body p-5 bg-white rounded">
    <p class="small mb-5 pb-lg-2"><a class="text-muted" href="{{ path('user_index') }}">Regresar al Inicio</a></p>     
     {% for message in app.flashes('error') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}

     {{ form_start(form) }}
            {{ form_row(form.password.first, { 'attr': {'class': 'form-control'} }) }}</br>
            {{ form_row(form.password.second, { 'attr': {'class': 'form-control'} }) }}

            <div class="register-btn-container">
  <u></u></a></p></div>
                <button class="btn btn-info btn-lg btn-block" type="submit">Actualizar!</button>
            </div>
     
        {{ form_end(form) }}
      
</div>
    {% endblock %}
